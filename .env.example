ELEVENLABS_API_KEY=
KIE_API_KEY=
# Optional — only the Frame.io native-speaker review stage needs this.
# Register a Frame.io / Adobe Developer Console app with assets-read +
# comments-read + upload scope. Leave blank to run review offline from an
# exported comments file (`review fetch --comments-json ...`).
FRAMEIO_TOKEN=
